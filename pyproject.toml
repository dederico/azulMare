[tool.poetry]
name = "general-call-gpt"
version = "0.1.0"
description = ""
authors = ["only-username-available <santiago.morillo@wizeline.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"